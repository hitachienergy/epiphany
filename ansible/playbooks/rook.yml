---
- hosts: repository
  gather_facts: true
  tasks: []

- hosts: kubernetes_master[0]
  become: true
  become_method: sudo
  roles:
    - rook
  environment:
    KUBECONFIG: "{{ kubeconfig.remote }}"
