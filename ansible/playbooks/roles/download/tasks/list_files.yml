---
- name: Get files list from the repository
  import_tasks: list_requirements.yml
  vars:
    _requirements: files

- name: Set files facts
  set_fact:
    list_files_result: "{{ list_requirements_result }}"
