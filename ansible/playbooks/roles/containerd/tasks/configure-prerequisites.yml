---
- name: Provide containerd.conf file
  copy:
    src: containerd.conf
    dest: /etc/modules-load.d/containerd.conf
    owner: root
    group: root
    mode: u=rw,go=

- name: Load modules
  command: modprobe {{ item }}
  loop:
    - overlay
    - br_netfilter
