"$id": "#/specification"
title: "Filebeat specification schema"
description: "Filebeat specification schema"
type: object
properties:
  postgresql_input:
    type: object
    properties:
      multiline:
        type: object
        properties:
          pattern:
            type: string
          negate:
            type: boolean
          match:
            type: string
