---
# Ansible playbook for backing up monitoring data

- hosts: prometheus[0]
  become: true
  become_method: sudo
  serial: 1
  tasks:
    - import_role:
        name: backup
        tasks_from: monitoring
  vars_files:
    - roles/prometheus/vars/main.yml
