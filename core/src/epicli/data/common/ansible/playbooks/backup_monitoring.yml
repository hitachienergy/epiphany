---
# Ansible playbook for backing up monitoring data

- hosts: prometheus
  become: true
  become_method: sudo
  serial: 1
  tasks:
    - import_role:
        name: backup
        tasks_from: monitoring
