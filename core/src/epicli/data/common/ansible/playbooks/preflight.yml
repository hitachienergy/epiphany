---
# Ansible playbook that checks if requirements are met

- hosts: all
  gather_facts: yes
  become: true
  become_method: sudo
  roles:
    - preflight
  environment:
    KUBECONFIG: "{{ kubeconfig.remote }}"
