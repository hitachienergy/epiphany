---
<<<<<<< HEAD
- name: Create timestamp value
  set_fact:
    timestamp: "{{ ansible_date_time.epoch }}"

- name: Create a backup directory
  file:
    path: "{{ backup_dir }}/postgresql_backup_{{ timestamp }}"
    state: directory
    mode: 0777

- name: Dump database
  command: runuser -l postgres -c 'pg_dumpall > {{ backup_dir }}/postgresql_backup_{{ timestamp }}/database_dump.sql'
=======
>>>>>>> upstream/feature/backup-restore
