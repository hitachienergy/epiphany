---

- name: Download file
  include_role:
    name: download
    tasks_from: download_image
  vars:
    file_name: "{{ image_name }}"

- name: Load image {{ image_name }}
  become: yes
  shell: "docker load --input {{ download_directory }}/{{ image_name }}"
