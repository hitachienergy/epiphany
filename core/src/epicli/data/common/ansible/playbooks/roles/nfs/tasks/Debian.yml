---
# Install required NFS packages for Debian based operating systems

- name: Ensure NFS packages are installed (Debian).
  apt:
    name:
      - nfs-common
      - nfs-kernel-server
    state: present
