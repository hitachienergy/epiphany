---
apiVersion: v1
kind: Service
metadata:
  name: {{ data.service.name }}
  labels:
    app: pgpool
  namespace: {{ data.namespace }}
spec:
  type: ClusterIP
  ports:
    - name: pgpool
      port: {{ data.service.port }}
      targetPort: 5432
      protocol: TCP
  selector:
    app: pgpool
