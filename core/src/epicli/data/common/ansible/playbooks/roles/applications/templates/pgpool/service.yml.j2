---
apiVersion: v1
kind: Service
metadata:
  name: {{ data.namespace }}
  labels:
    app: pgpool
  namespace: {{ data.service.name }}
spec:
  type: ClusterIP
  ports:
    - name: pgpool
      port: {{ data.service.port }}
      targetPort: 5432
      protocol: TCP
  selector:
    app: pgpool
