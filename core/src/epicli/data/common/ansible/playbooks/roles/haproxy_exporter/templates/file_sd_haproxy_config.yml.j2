# {{ ansible_managed }}

- targets: ['{{ inventory_hostname }}:{{ specification.config_for_prometheus.exporter_listen_port }}']
  labels:
{% for label, value in specification.config_for_prometheus.file_sd_labels.iteritems() %}
    "{{ label }}": "{{ value }}"
{% endfor %}

