---
pg_service_name:
  RedHat: postgresql-10
  Debian: postgresql

pg_config_dir:
  RedHat:  /var/lib/pgsql/10/data
  Debian: /etc/postgresql/10/main

repmgr_service_name:
  RedHat:  repmgr10
  Debian: repmgrd

repmgr_config_dir:
  RedHat:  /etc/repmgr/10
  Debian: /etc/postgresql/10/main