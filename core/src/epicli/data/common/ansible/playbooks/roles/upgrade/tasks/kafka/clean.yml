---
- name: Remove /opt/kafka
  file:
    path: /opt/kafka
    state: absent

- name: Remove old binaries
  file:
    path: /opt/kafka_{{ specification.kafka_var.scala.version }}-{{ specification.kafka_var.version }}
    state: absent
    
