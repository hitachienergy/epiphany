---
- name: Include install-Debian.yml
  include_tasks: "install-Debian.yml"
  vars:
    package_name: "{{ item }}"
  loop:
    - apt-transport-https
    - ca-certificates
    - containerd.io
    - docker-ce-cli
    - docker-ce