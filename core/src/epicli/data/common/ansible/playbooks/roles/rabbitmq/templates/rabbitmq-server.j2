listeners.tcp.default = {{ amqp_port }}

# To allow custom configs like vm_memory_high_watermark define it via custom_configurations in configuration/rabbitmq
{% for item in custom_configurations %}
{{ item.name }}                  = {{ item.value }} 
{% endfor %}
