---
- name: RabbitMQ | Add policies
  rabbitmq_policy:
    name: "{{ item.name }}"
    pattern: "{{ item.pattern }}"
    tags: "{{ item.definitions }}"
    state: present
  loop: "{{ rabbitmq_policies }}"
