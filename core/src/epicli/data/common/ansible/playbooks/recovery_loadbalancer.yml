---
# Ansible playbook for recovering loadbalancer config

- hosts: haproxy[0]
  become: true
  become_method: sudo
  serial: 1
  tasks:
    - import_role:
        name: recovery
        tasks_from: loadbalancer_haproxy_etc
  vars_files:
    - roles/haproxy/vars/main.yml
