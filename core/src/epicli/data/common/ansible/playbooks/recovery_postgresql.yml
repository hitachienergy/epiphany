---
# Ansible playbook for recovering Postgresql database

- hosts: postgresql[0]
  become: true
  become_method: sudo
  serial: 1
  tasks:
    - import_role:
        name: recovery
        tasks_from: postgresql
