# {{ ansible_managed }}

- targets: ['{{ inventory_hostname }}:{{ prometheus_node_exporter_web_listen_port }}']
  labels:
{% for label, value in prometheus_node_exporter_file_sd_labels.iteritems() %}
    "{{ label }}": "{{ value }}"
{% endfor %}
