---
name:
  type: string
  title: The Name Schema
  default: 'default'
  examples:
  - default
  pattern: ^[^\s]*$
title:
  type: string
  title: The Title Schema
  default: 'default'
  examples:
  - default
  pattern: ^(.*)$
provider:
  type: string
  title: The Provider Schema
  default: 'aws'
  examples:
  - aws
  - azure
  - any
  pattern: ^(aws|azure|any)$
version:
  type: string
  title: The Version Schema
  default: '0.4.2'
  examples:
  - 2.1.0+PGML
  - 2.1.0-rc.1+PGML
  - 2.1.0-dev+PGML
  # Ref: https://semver.org/spec/v2.0.0.html#is-there-a-suggested-regular-expression-regex-to-check-a-semver-string
  pattern: ^(?P<major>0|[1-9]\d*)\.(?P<minor>0|[1-9]\d*)\.(?P<patch>0|[1-9]\d*)(?:-(?P<prerelease>(?:0|[1-9]\d*|\d*[a-zA-Z-][0-9a-zA-Z-]*)(?:\.(?:0|[1-9]\d*|\d*[a-zA-Z-][0-9a-zA-Z-]*))*))?(?:\+(?P<buildmetadata>PGML))$
unvalidated_specification:
  type:
    - 'object'
    - 'null'
  title: The Specification Schema
